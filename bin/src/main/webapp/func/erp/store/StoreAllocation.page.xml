<?xml version="1.0" encoding="UTF-8"?>
<page xmlns="http://jboss.org/schema/seam/pages"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://jboss.org/schema/seam/pages http://jboss.org/schema/seam/pages-2.3.xsd">

    <param name="firstResult" value="#{allocationList.firstResult}"/>
    <param name="sort" value="#{allocationList.orderColumn}"/>
    <param name="dir" value="#{allocationList.orderDirection}"/>


    <param name="dateFrom" value="#{searchDateArea.dateFrom}"/>
    <param name="dateTo" value="#{searchDateArea.dateTo}"/>
    <param name="inStoreId" value="#{allocationList.inStoreId}"/>
    <param name="outStoreId" value="#{allocationList.outStoreId}"/>
    <param name="state" value="#{allocationList.allocationState}"/>
    <param name="reason" value="#{allocationList.reason}"/>
    <param name="type" value="#{allocationList.allocationType}"/>

</page>